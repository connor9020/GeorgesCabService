# Default configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/georgescabservice
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Service-specific configurations
cab-booking-service:
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/georgescabservice
      username: root
      password: root123

cab-fare-service:
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/georgescabservice
      username: root
      password: root123
